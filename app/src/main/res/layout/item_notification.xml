<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp"
    android:background="?android:attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true">

    <View
        android:id="@+id/unread_dot"
        android:layout_width="8dp"
        android:layout_height="8dp"
        android:background="@drawable/dot_unread_indicator"

        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/iv_notification_icon"
        app:layout_constraintBottom_toBottomOf="@id/iv_notification_icon"
        tools:visibility="visible"/>

    <ImageView
        android:id="@+id/iv_notification_icon"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_marginStart="16dp"
        android:background="@drawable/notification_icon_background"

        android:padding="6dp"
        app:layout_constraintStart_toEndOf="@id/unread_dot"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/ic_notifications"/>
    <TextView android:id="@+id/tv_notification_title" android:layout_width="0dp" android:layout_height="wrap_content" android:layout_marginStart="16dp" android:layout_marginEnd="8dp" android:textColor="@color/black" android:textStyle="bold" app:layout_constraintStart_toEndOf="@id/iv_notification_icon" app:layout_constraintEnd_toStartOf="@id/tv_notification_time" app:layout_constraintTop_toTopOf="@id/iv_notification_icon" tools:text="Nhắc nhở học tập"/>

    <TextView
        android:id="@+id/tv_notification_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#757575"
        android:textSize="12sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/tv_notification_title"
        app:layout_constraintBottom_toBottomOf="@id/tv_notification_title"
        tools:text="5 phút trước"/>

    <TextView
        android:id="@+id/tv_notification_content"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textColor="#333333"
        android:textSize="14sp"
        android:maxLines="2"
        android:ellipsize="end"
        app:layout_constraintStart_toStartOf="@id/tv_notification_title"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_notification_title"
        tools:text="Đã đến giờ học TOEIC Reading Practice (19:00). Bạn đã bỏ qua 2 task hôm nay."/>

</androidx.constraintlayout.widget.ConstraintLayout>