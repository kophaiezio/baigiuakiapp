<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/white"
    android:padding="24dp"
    tools:context=".NewPasswordActivity">

    <ImageView
        android:id="@+id/btn_new_pass_back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_arrow_left"
        android:padding="8dp"
        app:tint="@color/black" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Mật Khẩu Mới"
        android:textColor="@color/black"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_marginTop="32dp"/>


    <TextView android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="Phải có ít nhất 8 kí tự" android:textColor="#757575" android:textSize="12sp" android:layout_marginTop="8dp"/> <TextView android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="Phải có ít nhất một chữ cái, một số và một ký tự đặc biệt" android:textColor="#757575" android:textSize="12sp" app:drawableStartCompat="@drawable/ic_check_circle_filled"

    app:drawableTint="#4CAF50"

    android:drawablePadding="4dp" android:layout_marginTop="4dp"/>

    <EditText
        android:id="@+id/et_confirm_new_password"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="••••••••••"
        android:inputType="textPassword"
        android:background="@drawable/edittext_rounded_border"
        android:padding="12dp"
        android:layout_marginTop="16dp"
        app:drawableEndCompat="@drawable/ic_visibility_off"/>

    <Button
        android:id="@+id/btn_new_pass_continue"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Continue"
        android:backgroundTint="@color/colorButtonBlue"
        android:textColor="@color/white"
        android:paddingTop="12dp"
        android:paddingBottom="12dp"
        app:cornerRadius="12dp"
        android:layout_marginTop="24dp"/>

</LinearLayout>